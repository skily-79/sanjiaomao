## 团子 OCR 说明

请参考星河云（团子漫画 OCR）官方文档中的请求参数说明。

### 登录

第一次登录时可能会提示密码出错等问题，可以在确认正确输入后勾选并取消勾选 `force_refresh_token` 选项，以重新登录。保存后即可正常使用。

### 文本检测

文本检测功能也会提取出文字，而且是整体识别提取。所以当有使用团子的需求时，推荐不要单独使用 OCR 功能，而是使用团子的文本检测与 `none_ocr`。
团子有自带的拟声词过滤等功能，详细参数设置请参考上方的官方请求参数说明。
